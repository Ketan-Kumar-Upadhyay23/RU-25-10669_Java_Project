# Password Complexity Validator

This project is a simple Java application that validates password strength based on basic security requirements. It checks whether a password contains a minimum number of characters, at least one uppercase letter, one lowercase letter, one digit, and one special character.

The project demonstrates basic string processing and input validation techniques in Java and can serve as a foundation for implementing password policies in larger applications.
